[project]
name = "insectapi"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "huggingface-hub>=1.6.0",
    "pandas>=3.0.1",
    "pyarrow>=23.0.1",
    "uvicorn>=0.41.0",
]
